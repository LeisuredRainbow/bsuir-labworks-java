services:
  db:
    image: postgres:16-alpine
    container_name: travel-db
    environment:
      POSTGRES_DB: ${DB_NAME:-travel_agency}
      POSTGRES_USER: ${DB_USERNAME:-travel_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-travel_pass}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-travel_user} -d ${DB_NAME:-travel_agency}"]
      interval: 10s
      timeout: 5s
      retries: 10

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: travel-app
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-travel_agency}
      DB_USERNAME: ${DB_USERNAME:-travel_user}
      DB_PASSWORD: ${DB_PASSWORD:-travel_pass}
      PORT: 8080
      JAVA_OPTS: ${JAVA_OPTS:-}
    ports:
      - "${APP_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 40s

volumes:
  postgres_data: